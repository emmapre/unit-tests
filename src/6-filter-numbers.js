export const filterNumbers = (array, largerThan) => {

  //for each, behöver loopa igenom arrayen mot largerThan och kolla ifall det talet i arrayen är större än något i largerThan
  // if array =>

  return array
}